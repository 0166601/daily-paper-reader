---
title: Enhancing Cross-Problem Vehicle Routing via Federated Learning
title_zh: 通过联邦学习增强跨问题车辆路径规划
authors: "Xiangchi Meng, Jianan Zhou, Jie Gao, Yifan Lu, Yaoxin Wu, Gonglin Yuan, Yaqing Hou"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10652v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 针对复杂约束车辆路径规划问题的神经组合优化
tldr: 针对神经组合优化在处理不同约束的车辆路径问题（VRP）时存在的性能下降和泛化性差的问题，本文提出了一种基于联邦学习的“多问题预训练，单问题微调”（MPSF-FL）框架。该框架利用联邦全局模型促进跨问题的知识共享，使局部模型在保留通用VRP知识的同时，能高效适配具有异构复杂约束的下游任务，显著提升了模型在多样化及未知问题上的表现。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10652v1/fig-001.webp\", \"caption\": \"Figure 3: Our “Multi-problem Pre-train, then Single-problem Fine-tune” framework with Federated Learning (MPSF-FL) for VRPs, taking MVMoE as a model example. The G and {E1, E2, E3, E4} in the figure represent the gating network and expert network in the mixture-ofexpert moudle of MVMoE, which handles different problem features.\", \"page\": 4, \"index\": 1, \"width\": 802, \"height\": 612}, {\"url\": \"assets/figures/arxiv/2604.10652v1/fig-002.webp\", \"caption\": \"Table 3: Client-side performance comparison in data-scarce scenario.\", \"page\": 7, \"index\": 2, \"width\": 1052, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2604.10652v1/fig-003.webp\", \"caption\": \"Table 2: Client-side adaptation performance comparison for Cada on 1K test instances. The best performance is highlighted in bold, and the second-best is underlined.\", \"page\": 7, \"index\": 3, \"width\": 1052, \"height\": 345}, {\"url\": \"assets/figures/arxiv/2604.10652v1/fig-004.webp\", \"caption\": \"Table 1: Client-side performance comparison for MVMoE on 1K test instances. The best performance is highlighted in bold, and the second-best is underlined.\", \"page\": 6, \"index\": 4, \"width\": 1050, \"height\": 358}]"
motivation: 现有的神经组合优化方法在从简单VRP变体迁移到具有复杂约束的问题时，面临性能退化和泛化能力不足的挑战。
method: 提出MPSF-FL框架，通过联邦学习机制在全局模型中积累通用知识，并引导局部模型进行针对特定问题的微调。
result: 实验结果表明，该框架不仅增强了模型在多种VRP变体上的性能，还显著提升了其对未见问题的泛化能力。
conclusion: 联邦学习与预训练-微调模式的结合，为解决跨问题车辆路径优化提供了一种高效且鲁棒的新范式。
---

## 摘要
车辆路径问题 (VRPs) 是现代物流和供应链管理中的核心优化挑战。近期的神经组合优化 (NCO) 已展现出优于某些传统算法的效率。虽然作为解决通用 VRP 的主要 NCO 方法，当前的跨问题学习范式在从简单 VRP 变体迁移到涉及不同且复杂约束的变体时，仍面临性能下降和泛化能力衰减的问题。为了强化这些范式，本文提出了一种创新的“多问题预训练，随后单问题微调”联邦学习框架 (MPSF-FL)。该框架利用联邦全局模型的通用知识，促进本地模型在进行单问题微调时实现高效的跨问题知识共享与迁移。通过这种方式，本地模型能够有效地保留来自最新全局模型的通用 VRP 知识，同时高效地适应具有异构复杂约束的下游 VRP。实验结果表明，我们的框架不仅提升了在多种 VRP 中的性能，还提高了在未知问题上的泛化能力。

## Abstract
Vehicle routing problems (VRPs) constitute a core optimization challenge in modern logistics and supply chain management. The recent neural combinatorial optimization (NCO) has demonstrated superior efficiency over some traditional algorithms. While serving as a primary NCO approach for solving general VRPs, current cross-problem learning paradigms are still subject to performance degradation and generalizability decay, when transferring from simple VRP variants to those involving different and complex constraints. To strengthen the paradigms, this paper offers an innovative "Multi-problem Pre-train, then Single-problem Fine-tune" framework with Federated Learning (MPSF-FL). This framework exploits the common knowledge of a federated global model to foster efficient cross-problem knowledge sharing and transfer among local models for single-problem fine-tuning. In this way, local models effectively retain common VRP knowledge from up-to-date global model, while being efficiently adapted to downstream VRPs with heterogeneous complex constraints. Experimental results demonstrate that our framework not only enhances the performance in diverse VRPs, but also improves the generalizability in unseen problems.

---

## 论文详细总结（自动生成）

这是一份关于论文《Enhancing Cross-Problem Vehicle Routing via Federated Learning》的结构化深入分析总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：车辆路径问题（VRP）是物流领域的核心挑战。虽然神经组合优化（NCO）在效率上优于传统算法，但现有模型通常针对特定VRP变体独立训练，缺乏通用性。
*   **核心问题**：现有的“跨问题学习”（CPL）范式在从简单问题（如CVRP）迁移到具有复杂约束（如时间窗、回程、开放路径等组合）的变体时，性能会大幅下降。此外，针对特定变体进行微调往往会导致模型对该变体过拟合，从而丧失在其他VRP变体上的泛化能力。
*   **研究动机**：利用联邦学习（FL）机制，在不共享私有数据的前提下，通过全局模型汇聚多种VRP变体的通用知识，从而同时提升局部模型在特定任务上的性能和在未知任务上的泛化性。

### 2. 论文提出的方法论：MPSF-FL 框架
该论文提出了一种名为 **MPSF-FL**（Multi-problem Pre-train, then Single-problem Fine-tune with Federated Learning）的框架，主要包含两个阶段：

*   **阶段一：多问题预训练（Multi-problem Pre-train）**
    *   在中心服务器上，使用多种简单的 VRP 变体（如 CVRP, OVRP, VRPL 等）训练一个统一的神经求解器（如 MVMoE 或 CaDA），旨在捕获 VRP 的通用表征（如局部结构和路径连通性）。
*   **阶段二：单问题联邦微调（Single-problem Fine-tune）**
    *   **局部微调**：N 个客户端分别代表不同的复杂 VRP 变体，利用本地私有数据对预训练模型进行微调。
    *   **全局聚合与模型合并（关键技术）**：为了解决多任务聚合时的参数冲突，引入了 **TIES-merging** 策略：
        1.  **Trim（修剪）**：仅保留每个客户端更新向量中绝对值最大的前 $\kappa\%$ 的参数，过滤噪声。
        2.  **Sign Voting（符号投票）**：确定每个参数维度的全局更新方向（取大多数客户端一致的方向）。
        3.  **Disjoint Merge（不相交合并）**：仅对方向与全局一致的参数进行平均计算，剔除干扰项。

### 3. 实验设计
*   **数据集与场景**：
    *   **预训练任务**：6 种简单 VRP 变体。
    *   **微调/测试任务**：10 种复杂的组合约束 VRP 变体（如 OVRPBLTW 等）。
    *   **问题规模**：节点数 $n=50$ 和 $n=100$。
*   **Benchmark（基准）**：
    *   **传统求解器**：PyVRP (基于 HGS-CVRP) 和 Google OR-Tools。
*   **对比方法**：
    *   **MVMoE-pretrain / CaDA-pretrained**：仅预训练后的零样本表现。
    *   **MVMoE-CPL / CaDA-CPL**：传统的单任务独立微调。
    *   **MVMoE-MTF**：集中式多任务微调（假设可以访问所有数据，作为性能上限）。

### 4. 资源与算力
*   **硬件环境**：Intel(R) Xeon(R) Gold 5120 CPU @ 2.20GHz，**NVIDIA Tesla V100-SXM2-32GB GPU**。
*   **训练细节**：
    *   MVMoE 预训练 5000 轮，CaDA 预训练 300 轮。
    *   联邦学习阶段：共 20 轮通信，每轮客户端本地训练 5 个 Epoch（总计 100 Epoch，与独立微调基准对齐）。
    *   数据量：每个 Epoch 使用 20,000 个实例（数据稀缺实验中限制为总共 20,000 个）。

### 5. 实验数量与充分性
*   **实验规模**：论文在 10 种不同的复杂 VRP 变体上进行了测试，涵盖了两种不同的先进统一模型架构（MVMoE 和 CaDA），并对比了两种问题规模（50 和 100 节点）。
*   **充分性与公平性**：
    *   进行了**数据稀缺性实验**，模拟现实中私有数据不足的场景。
    *   所有对比方法在预训练阶段、微调轮数和超参数设置上保持了高度一致，确保了对比的公平性。
    *   使用了 1000 个测试实例并报告了平均 Objective 和 Gap，结果具有统计说服力。

### 6. 主要结论与发现
*   **泛化性显著增强**：MPSF-FL 训练的局部模型在“未见任务”上的表现远优于传统 CPL 微调，有效缓解了微调导致的泛化能力衰减。
*   **性能接近集中式训练**：在保护隐私的前提下，联邦学习的效果接近甚至在某些任务上超过了能够访问全量数据的集中式多任务微调（MTF）。
*   **鲁棒性强**：在数据稀缺（仅 2 万条数据）的情况下，联邦聚合起到了正则化作用，防止局部模型陷入局部最优，表现比独立微调更稳健。
*   **通用性**：该框架可无缝集成到不同的 NCO 架构（如 MVMoE 和 CaDA）中。

### 7. 优点与亮点
*   **创新性地结合了 FL 与 NCO**：解决了 NCO 领域长期存在的“任务特化 vs. 通用泛化”的矛盾。
*   **引入 TIES-merging**：针对 VRP 任务异构性强的特点，采用了比简单平均（FedAvg）更先进的参数合并策略，有效处理了参数冲突。
*   **隐私保护**：符合现代物流企业不愿共享核心业务数据的现实需求。

### 8. 不足与局限
*   **计算开销**：虽然联邦学习提升了效果，但相比简单的单任务微调，它需要多轮通信和服务器端的聚合操作，增加了系统复杂性。
*   **客户端同质化假设**：实验中假设所有客户端使用相同的模型架构，未探讨异构模型架构下的联邦优化。
*   **问题规模限制**：实验主要集中在 100 节点以内，对于超大规模（如 1000+ 节点）的复杂约束 VRP，其性能表现和通信压力尚待验证。
*   **超参数敏感性**：TIES-merging 中的修剪比例 $\kappa$ 和缩放因子 $\lambda$ 可能需要针对不同模型进行精细调优。

（完）
