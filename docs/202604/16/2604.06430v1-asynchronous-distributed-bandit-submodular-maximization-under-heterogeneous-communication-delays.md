---
title: Asynchronous Distributed Bandit Submodular Maximization under Heterogeneous Communication Delays
title_zh: 异构通信延迟下的异步分布式 Bandit 子模最大化
authors: "Pranjal Sharma, Zirui Xu, Vasileios Tzoumas"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06430v1"
tags: ["query:q10"]
score: 6.0
evidence: 异构延迟下的异步分布式决策
tldr: 本文研究了异构通信延迟下的异步分布式多智能体老虎机子模最大化问题。针对现有方法依赖同步时钟和同构延迟的局限性，提出一种异步协调算法，允许智能体在时钟不匹配且仅与邻居通信的情况下进行决策。理论证明了该算法具有可观的近似保证，且误差界限与延迟、时钟偏差及拓扑结构明确相关，并通过多摄像头监控仿真验证了有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.06430v1/fig-001.webp\", \"caption\": \"Fig. 1: Simulation layout and sample snapshot of camera (black vertices) and target (black crosses) configuration. 16 cameras are placed on a 4× 4 grid over a 100× 100 workspace. Each camera has a sector FOV with half-angle 30◦ and sensing range of 20 units (light gray wedges show the selected heading of each camera). Colored edges denote the one-hop communication links between grid neighbors, with warmer colors representing higher delays.\", \"page\": 6, \"index\": 1, \"width\": 437, \"height\": 377}, {\"url\": \"assets/figures/arxiv/2604.06430v1/fig-002.webp\", \"caption\": \"Fig. 2: Coverage over time (mean ±95% CI, n = 20 runs, running average over 50 time steps) for DOG-IU and DOG under increasing maximum one-hop delay d̄ on a 100× 100 workspace with 16 grid-placed cameras, 8 headings, and 80 clustered targets (8 clusters). The gap widens as delay increases, illustrating the benefit of intermediate updates under more severe communication delays.\", \"page\": 7, \"index\": 2, \"width\": 1052, \"height\": 444}]"
motivation: 现有的分布式决策方法通常假设全局同步时钟和同构通信延迟，难以应对实际应用中异构延迟和时钟不匹配带来的挑战。
method: 提出一种异步协调算法，使智能体仅通过与一阶邻居交换信息即可在存在通信延迟和本地时钟偏差的环境下进行决策。
result: 理论上建立了相对于最优同步中心化方案的近似保证，并明确了子优性差距与通信延迟、时钟偏差及邻域拓扑的关系。
conclusion: 该算法有效解决了异步分布式环境下的子模最大化问题，为大规模多智能体系统在未知环境中的信息采集提供了可扩展的解决方案。
---

## 摘要
我们研究了面向可扩展多智能体 Bandit 子模最大化的异步分布式决策问题。本研究的动机源于未知环境及智能体间存在异构通信延迟下的分布式信息采集任务。为了在有限的通信延迟下实现可扩展性，现有方法通常限制每个智能体仅与其一跳邻居进行协调。然而，这些方法假设智能体之间具有同构的通信延迟以及同步的全局时钟。在实际应用中，延迟往往是异构的，且智能体在不匹配的本地时钟下运行。这意味着每个智能体无法同时接收来自所有邻居的信息，从而影响决策质量。在本文中，我们提出了一种异步协调算法来应对这些挑战。我们建立了相对于最优同步集中式解的可证明近似保证，其中次优性间隙显式地取决于通信延迟和时钟失配。此外，该界限还取决于每个邻域的拓扑结构，从而刻画了仅通过一跳邻域消息进行分布式决策的影响。我们通过多摄像头区域监控的数值模拟验证了该方法的有效性。

## Abstract
We study asynchronous distributed decision-making for scalable multi-agent bandit submodular maximization. We are motivated by distributed information-gathering tasks in unknown environments and under heterogeneous inter-agent communication delays. To enable scalability despite limited communication delays, existing approaches restrict each agent to coordinate only with its one-hop neighbors. But these approaches assume homogeneous communication delays among the agents and a synchronous global clock. In practice, however, delays are heterogeneous, and agents operate with mismatched local clocks. That is, each agent does not receive information from all neighbors at the same time, compromising decision-making. In this paper, we provide an asynchronous coordination algorithm to overcome the challenges. We establish a provable approximation guarantee against the optimal synchronized centralized solution, where the suboptimality gap explicitly depends on communication delays and clock mismatches. The bounds also depend on the topology of each neighborhood, capturing the effect of distributed decision-making via one-hop-neighborhood messages only. We validate the approach through numerical simulations on multi-camera area monitoring.