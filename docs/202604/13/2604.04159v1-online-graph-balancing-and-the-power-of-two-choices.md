---
title: Online Graph Balancing and the Power of Two Choices
title_zh: 在线图平衡与二选一的力量
authors: "Nikhil Bansal, Milind Prabhu, Sahil Singla, Siddharth M. Sundaram"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04159v1"
tags: ["query:q3"]
score: 6.0
evidence: 边顺序到达的在线图平衡问题
tldr: 本文研究了i.i.d.模型下的在线图平衡问题，即在已知基础图的情况下，如何实时定向随机到达的边以最小化最大入度。虽然贪婪算法在完全图上能达到O(log log n)的竞争比，但在一般非正则图中表现不佳。作者提出了一种基于“对数偏斜度”概念的新算法，通过将基础图分解为多个偏斜双正则子图，在任意基础图上均实现了最优的O(log log n)竞争比，成功将“两次选择的力量”推广到了更广泛的图结构中。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04159v1/fig-001.webp\", \"caption\": \"Figure 1: Imbalanced biregular obstruction motivating log-skewness. A large low-degree side A and a small high-degree side B create, after sampling, a dense core of load Ω(s).\", \"page\": 8, \"index\": 1, \"width\": 961, \"height\": 350}, {\"url\": \"assets/figures/arxiv/2604.04159v1/fig-002.webp\", \"caption\": \"Figure 2: Left: a rooted pattern, with edge colors indicating decomposition classes. Right: a sampled graph containing a tree of this pattern. Thick colored edges are greedy, and thin dotted edges are threshold. Black circles are left vertices and gray circles are right vertices. Each left vertex incident to a class-i greedy edge has at least αi class-i threshold edges.\", \"page\": 15, \"index\": 2, \"width\": 844, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2604.04159v1/fig-003.webp\", \"caption\": \"Figure 3: Lower-bound construction for Greedy. The graph has layers V1, . . . , Vb with |Vi| = ti √ n, where t = (logn)3, and between consecutive layers the edges form ti disjoint copies of K√ n, √ nt. The figure shows the batch-wise propagation of load by Greedy: batch B1 pushes all of Vb−1 to load at least 1, batch B2 pushes all of Vb−2 to load at least 2, and so on.\", \"page\": 18, \"index\": 3, \"width\": 686, \"height\": 309}]"
motivation: 旨在探索在i.i.d.边到达模型下，能否在任意基础图上实现优于贪婪算法的O(log log n)竞争比。
method: 引入“对数偏斜度”概念来刻画图的非正则性，并设计了一种基于图分解的改进型贪婪算法。
result: 证明了传统贪婪算法在某些非正则图中表现较差，而新算法在任何基础图上都能达到O(log log n)的最优竞争比。
conclusion: 该研究揭示了图结构对在线平衡的影响，并为处理非均匀随机负载平衡问题提供了新的理论框架。
---

## 摘要
在经典的在线图平衡问题中，边按顺序到达，且必须在到达时立即定向，以最小化最大入度。对于对抗性到达，自然的贪婪算法是 $O(\log n)$-竞争的，且该界限对于任何算法（即使是随机化算法）都是最优的。我们在独立同分布（i.i.d.）模型下研究该问题，其中基础图 $G$ 预先已知，且每次到达的边都是从 $G$ 中独立均匀随机抽取的。该模型推广了标准的“二选一”设置（对应于 $G = K_n$），在这种设置下，贪婪算法可以达到 $O(\log\!\log n)$ 的保证。我们探究对于任意基础图是否可能获得类似的界限。虽然贪婪算法对于对抗性到达以及正则基础图（如 $G = K_n$）的 i.i.d. 到达是最优的，但我们证明它在一般情况下表现不佳：存在轻微非正则的图 $G$，使得贪婪算法在 i.i.d. 到达下是 $\widetildeΩ(\log n)$-竞争的。与之形成鲜明对比的是，我们的主要结果是针对每个基础图 $G$ 提出了一种 $O(\log\!\log n)$-竞争的在线算法；这在常数因子范围内是最优的，因为即使对于完全图 $G = K_n$，$\Omega(\log\!\log n)$ 的下界也已经成立。关键的新思路是图的“对数偏斜度”（log-skewness）概念，它捕捉了 $G$ 中迫使离线最优解变大的非正则子结构。此外，我们证明任何基础图都可以在仅 $O(\log\!\log n)$ 个对数偏斜度尺度上分解为“偏斜双正则”（skew-biregular）部分，并利用这一点设计了一种基于分解的贪婪算法变体，该算法是 $O(\log\!\log n)$-竞争的。

## Abstract
In the classic online graph balancing problem, edges arrive sequentially and must be oriented immediately upon arrival, to minimize the maximum in-degree. For adversarial arrivals, the natural greedy algorithm is $O(\log n)$-competitive, and this bound is the best possible for any algorithm, even with randomization. We study this problem in the i.i.d. model where a base graph $G$ is known in advance and each arrival is an independent uniformly random edge of $G$. This model generalizes the standard power-of-two choices setting, corresponding to $G = K_n$, where the greedy algorithm achieves an $O(\log\!\log n)$ guarantee. We ask whether a similar bound is possible for arbitrary base graphs.   While the greedy algorithm is optimal for adversarial arrivals and also for i.i.d. arrivals from regular base graphs (such as $G = K_n$), we show that it can perform poorly in general: there exist mildly irregular graphs $G$ for which greedy is $\widetildeΩ(\log n)$-competitive under i.i.d. arrivals. In sharp contrast, our main result is an $O(\log\!\log n)$-competitive online algorithm for every base graph $G$; this is optimal up to constant factors, since an $Ω(\log\!\log n)$ lower bound already holds even for the complete graph $G = K_n$. The key new idea is a notion of log-skewness for graphs, which captures the irregular substructures in $G$ that force the offline optimum to be large. Moreover, we show that any base graph can be decomposed into ``skew-biregular'' pieces at only $O(\log\!\log n)$ scales of log-skewness, and use this to design a decomposition-based variant of greedy that is $O(\log\!\log n)$-competitive.