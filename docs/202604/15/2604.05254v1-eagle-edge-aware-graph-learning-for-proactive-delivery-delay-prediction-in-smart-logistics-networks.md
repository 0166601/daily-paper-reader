---
title: "EAGLE: Edge-Aware Graph Learning for Proactive Delivery Delay Prediction in Smart Logistics Networks"
title_zh: EAGLE：用于智能物流网络主动式配送延迟预测的边感知图学习
authors: "Zhiming Xue, Menghao Huo, Yujue Wang"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05254v1"
tags: ["query:q3"]
score: 9.0
evidence: 利用图学习在智能物流网络中进行主动配送延迟预测
tldr: 针对现代物流网络中交付延迟预测滞后的问题，本文提出EAGLE框架。该框架结合了轻量级Transformer补丁编码器和边缘感知图注意力网络（E-GAT），同时建模订单流的时间动态和物流枢纽间的空间依赖。在DataCo数据集上的实验表明，EAGLE在预测准确率和训练稳定性方面均优于现有方法，为主动式供应链风险管理提供了有效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05254v1/fig-001.webp\", \"caption\": \"Figure 2: EAGLE training dynamics across 4 random seeds. (a) Training loss converges smoothly to ≈ 0.186 by epoch 40. (b) Validation AUC-ROC stabilises above 0.97 after 15 epochs. Stars mark the best-checkpoint epoch per seed.\", \"page\": 5, \"index\": 1, \"width\": 950, \"height\": 325}, {\"url\": \"assets/figures/arxiv/2604.05254v1/fig-002.webp\", \"caption\": \"Table 3: Feature audit. Temporal bounds and source for features are recorded to ensure that outcome information does not leak into the features.\", \"page\": 6, \"index\": 2, \"width\": 1027, \"height\": 258}, {\"url\": \"assets/figures/arxiv/2604.05254v1/fig-003.webp\", \"caption\": \"Figure 1: EAGLE three-module architecture pipeline. (1) PatchTST-Lite: Each node’s 𝑇=14 time-step feature sequence is tokenised into 𝑁𝑝=2 non-overlapping patches of length 𝑝=7, encoded by a 2-layer Transformer (2 attention heads, 𝑑model=64), and mean-pooled to a fixed temporal embedding h𝑡 ∈ R64. (2) E-GAT: A 2-layer Edge-Aware GAT (4 heads per layer) aggregates neighbourhood information, conditioning attention coefficients jointly on node embeddings and static edge features E𝑢𝑣 (historical transit statistics, shipping mode distributions). (3) DualHead: Two independent MLP heads produce the SLA-violation probability 𝑝𝑣 ∈ 0, 1 (sigmoid) and expected delay magnitude 𝑑𝑣 ∈ R (Softplus), trained end-toend via a 0.7×BCE 0.3×Huber multi-task loss.\", \"page\": 2, \"index\": 3, \"width\": 356, \"height\": 884}]"
motivation: 现有的物流延迟预测方法往往忽略了物流网络的拓扑结构或供应链中的空间依赖性，导致预测缺乏前瞻性。
method: 提出一种混合深度学习框架，利用Transformer处理时间序列动态，并结合边缘感知图注意力网络（E-GAT）捕捉节点间的关系特征。
result: 在真实数据集上达到了0.8762的F1分数和0.9773的AUC-ROC，且训练稳定性比消融变体提升了3.8倍。
conclusion: 该研究证明了结合时空特征与图学习能显著提升物流延迟预测的精度与鲁棒性，助力实现主动式供应链风险管理。
---

## 摘要
现代物流网络在每个仓库节点和运输路线上都会产生丰富的运营数据流——从订单时间戳、路由记录到货运清单——然而，配送延迟预测目前仍主要处于被动响应阶段。现有的预测方法通常将该问题视为表格分类任务（忽略了网络拓扑结构），或者视为时间序列异常检测任务（忽视了供应链图的空间依赖性）。为了弥补这一差距，我们提出了一种用于主动式供应链风险管理的混合深度学习框架。该方法通过轻量级 Transformer patch 编码器对时间维度的订单流动态进行建模，并通过边感知图注意力网络（E-GAT）对枢纽间的关系依赖进行建模，并利用多任务学习目标进行优化。在真实的 DataCo 智能供应链数据集上的评估结果显示，我们的框架相比基准方法取得了持续的改进，F1 分数达到 0.8762，AUC-ROC 达到 0.9773。在四个独立的随机种子实验中，该框架的跨种子 F1 标准差仅为 0.0089（比表现最好的消融变体提升了 3.8 倍），在所有评估模型中实现了预测准确性与训练稳定性之间的最佳平衡。

## Abstract
Modern logistics networks generate rich operational data streams at every warehouse node and transportation lane -- from order timestamps and routing records to shipping manifests -- yet predicting delivery delays remains predominantly reactive. Existing predictive approaches typically treat this problem either as a tabular classification task, ignoring network topology, or as a time-series anomaly detection task, overlooking the spatial dependencies of the supply chain graph. To bridge this gap, we propose a hybrid deep learning framework for proactive supply chain risk management. The proposed method jointly models temporal order-flow dynamics via a lightweight Transformer patch encoder and inter-hub relational dependencies through an Edge-Aware Graph Attention Network (E-GAT), optimized via a multi-task learning objective. Evaluated on the real-world DataCo Smart Supply Chain dataset, our framework achieves consistent improvements over baseline methods, yielding an F1-score of 0.8762 and an AUC-ROC of 0.9773. Across four independent random seeds, the framework exhibits a cross-seed F1 standard deviation of only 0.0089 -- a 3.8 times improvement over the best ablated variant -- achieving the strongest balance of predictive accuracy and training stability among all evaluated models.

---

## 论文详细总结（自动生成）

这是一份关于论文《EAGLE: Edge-Aware Graph Learning for Proactive Delivery Delay Prediction in Smart Logistics Networks》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：现代物流网络虽然产生了海量的 IoT 追踪数据和订单流数据，但目前的配送延迟管理仍处于“被动响应”状态。
*   **核心痛点**：
    1.  **空间依赖缺失**：传统表格模型（如 XGBoost）忽略了物流枢纽间的拓扑关系。
    2.  **时间动态不足**：现有时空图神经网络（GNN）往往缺乏对季节性订单流动态的显式建模。
    3.  **训练不稳定**：图模型在处理类别不平衡的物流数据时，跨随机初始化的预测结果波动巨大。
*   **研究目标**：提出一种混合深度学习框架，通过解耦时间与结构建模，实现高精度且高稳定性的主动延迟预测。

### 2. 方法论
EAGLE 框架由三个核心模块组成：
*   **PatchTST-Lite 时间编码器**：
    *   采用通道独立（Channel-independent）的 Transformer 架构。
    *   将 14 天的时间序列切分为非重叠的补丁（Patches），通过 2 层 Transformer 提取时间特征，旨在捕捉双周季节性并减少局部噪声干扰。
*   **边感知图注意力网络 (E-GAT)**：
    *   **核心思想**：在计算节点间的注意力权重时，不仅考虑节点特征（订单量、历史延迟），还显式引入**边特征**（运输模式、历史航线统计）。
    *   **公式逻辑**：注意力系数 $\alpha_{uv}$ 由源节点、目标节点和边特征三者共同决定，从而更精准地模拟物流路径上的风险传播。
*   **多任务学习与双头预测**：
    *   **分类头**：预测 SLA（服务等级协议）违约概率（Sigmoid）。
    *   **回归头**：预测具体的延迟天数（Softplus）。
    *   **损失函数**：采用 $0.7 \times \text{BCE} + 0.3 \times \text{Huber}$ 的组合。回归任务作为辅助任务，为模型提供更密集的梯度信号，增强正则化效果。

### 3. 实验设计
*   **数据集**：使用公开的 **DataCo Smart Supply Chain** 数据集，包含 180,519 条订单记录。构建了一个包含 46 个地理节点和 1478 条有向边的物流图。
*   **评估协议**：采用严格的“下一窗口”协议（用 $t$ 到 $t+14$ 天的数据预测 $t+14$ 到 $t+28$ 天的结果），彻底杜绝时间泄露。
*   **对比基准 (Baselines)**：
    *   **表格模型**：XGBoost、Random Forest（代表非图学习的最优水平）。
    *   **时间序列模型**：LSTM（仅考虑时间维度）。
    *   **图学习模型**：标准 GAT（静态图拓扑，无显式时间编码）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量及训练总时长。
*   **实现细节**：提到使用 PyTorch 和 PyTorch Geometric 框架，采用 AdamW 优化器，学习率为 0.0003，并使用了余弦退火（Cosine Annealing）调度策略。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **主实验**：在 4 个独立随机种子下运行，报告了均值和标准差，验证了稳定性。
    *   **消融实验**：设计了三组变体（无时间编码 A1、无边特征 A2、单任务学习 A3），量化了各模块贡献。
    *   **可解释性实验**：利用 SHAP 值进行特征归因，并绘制了风险热力图进行案例分析。
*   **充分性评价**：实验设计较为客观，特别是针对 DataCo 数据集常见的“标签泄露”问题进行了专门的特征审计（Feature Audit），确保了结果的真实性。

### 6. 主要结论与发现
*   **性能领先**：EAGLE 达到了 **0.8762 的 F1 分数**和 **0.9773 的 AUC-ROC**，显著优于所有基准模型。
*   **稳定性突破**：EAGLE 的跨种子 F1 标准差仅为 0.0089，比表现最接近的图模型变体稳定了 **3.8 倍**。
*   **能力层级**：实验证明了“表格 < 时间序列 < 时间+图混合”的能力演进路径，证实了空间结构信息能提供时间建模之外的补充信号。

### 7. 优点
*   **边感知机制**：将运输航线的静态属性（如运输模式）融入注意力机制，符合物流实际业务逻辑。
*   **高鲁棒性**：通过 Patch 编码和多任务学习，克服了 GNN 在稀疏/不平衡数据下难以收敛和不稳定的顽疾。
*   **严谨的防泄露设计**：详细列出了特征审计表，解决了该领域研究中常见的因特征/标签共源导致性能虚高的问题。

### 8. 不足与局限
*   **计算开销**：相比于轻量级的 LSTM 或 XGBoost，混合 Transformer 和 GNN 的架构计算复杂度较高，大规模实时部署可能面临挑战。
*   **冷启动限制**：模型依赖历史延迟基准（$\mu_v$）来定义相对延迟标签，对于完全没有历史数据的新节点，预测效能可能会下降。
*   **泛化验证**：目前仅在 DataCo 单一数据集上验证，不同拓扑结构（如极度稀疏或极度稠密）的物流网络对其性能的影响尚待观察。

（完）
